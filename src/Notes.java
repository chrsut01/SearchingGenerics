
/*
Search search = new Search(new SequentialSearch());

        search.setStrategy(new SequentialSearch());

        search.doSearch();

        Animal rr = new Animal("roadrunner");

        // set strategy 1

        rr.setStrategy(new NormalPattern());

        //run

        rr.run(3);

        Context binSearch = new Context(new BinarySearch());*/






/*
        List<Person> l1 = generateRandomPersonList(1000);
        List<Person> l2 = generateRandomPersonList(10000000);

       // Search search = new Search(new SequentialSearch());

        //search.setStrategy(new SequentialSearch());

        //search.doSearch();


        // to print out all Persons in list:
       // for(Person person : l1)
        //System.out.println(person);


    }
    private static List<Person> generateRandomPersonList(int size) {
        List<Person> list = new ArrayList<>();
        Random random = new Random();
        for (int i = 0; i < size; i++) {
            Person person = new Person("Person " + i, random.nextInt(10000000,99999999));
            list.add(person);
        }
        return list;*/
